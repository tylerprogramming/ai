@AGENTS.md

## Claude Code specifics

- Use plan mode before adding a new demo file. Show the file list first.
- When asked to set up the self-hosted demo, follow `self_hosted/README.md`
  in order. You can run `create_session.py` and `stream.py`. You cannot make
  the environment key or export `CODEX_API_KEY`; stop and hand those two
  steps to the user.
